# Particle specific notes

## Compile and flash

Drop into the particle console and run

>Particle compile boron 

To use the cloud compiler but get a local binary. Then flash to target name device
>Particle flash $name